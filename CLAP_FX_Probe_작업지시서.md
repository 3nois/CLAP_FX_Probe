# CLAP FX Probe — 작업 지시서 (개정판)

> Claude Code에 그대로 붙여넣으세요.

---

## 배경

논문 **TokenSynth** (arXiv:2502.08939)는 MIDI 토큰 + CLAP 음색 임베딩으로 오디오 토큰을 생성하는 neural synthesizer다. 이 논문에는 설명되지 않은 실패가 하나 있다.

오디오 이펙트(EQ·디스토션·리버브)로 augmentation한 데이터로 학습한 `TokenSynth-Aug`가, 정작 이펙트 걸린(wet) 오디오를 복제할 때 **dry로만 학습한 기본 모델보다 못했다.** 논문은 원인을 이렇게 적었다.

> "CLAP 모델로부터 얻은 음색 임베딩이 오디오 이펙트에 대한 정보를 결여하고 있기 때문으로 **보인다(likely due to)**."

**저자들은 이걸 측정하지 않았다.** 청취 테스트로 실패를 관찰하고 원인을 추정했을 뿐, 논문 어디에도 CLAP 임베딩을 직접 조사한 흔적이 없다.

이 프로젝트는 그것을 직접 측정한다.

---

## 가설과 판정 기준 — 구조의 위계

원래 설계는 "차이 벡터들이 같은 방향인가"라는 단일 질문(H1에 해당)만 봤다. 이건 너무
엄격한 질문이다 — 방향이 소스마다 제각각이어도 어떤 방법으로든 예측 가능하면 실용적으로
쓸 수 있다. 그래서 단일 가설을 **구조의 위계**로 재구성한다.

| 단계 | 형태 | 의미 | 실용적 귀결 |
|---|---|---|---|
| **H0** | 정보 없음 | 아무 방법으로도 못 읽음 | 별도 이펙트 인코더를 병용해야 함 |
| **H1** | `e' = e + v` | 상수 벡터. 가장 강한 구조 | 벡터 하나만 더하면 됨 — 사실상 무료 |
| **H2** | `e' = e + f(p)·v` | 방향 고정, 크기만 파라미터 의존 | 스칼라 함수 f(p) 하나만 fit하면 됨 |
| **H3** | `e' = e + Δ(p)` | 파라미터별 방향. 소스와는 무관 | 파라미터값→벡터 룩업/작은 함수로 충분 |
| **H4** | `e' = W·e + b` | 선형 변환. 소스에 따라 다르게 움직임 | 작은 선형 계층 하나만 추가하면 됨 |
| **H5** | `e' = e + g(e, p)` | 비선형. MLP로 학습 | MLP 추론 1회 (거의 무료) — 여전히 실용적 |
| **H6** | 정보는 있으나 학습 불가 | 사실상 H0 | 별도 이펙트 인코더 필요 (H0과 동일 결론) |

**핵심: H1이 깨져도 실험이 끝나지 않는다.** 어느 칸에서 구조가 잡히는지 찾는 것이 목표다.
H5에서 잡혀도 실용성은 유지된다 — MLP 추론 한 번은 비용이 없다시피 하기 때문이다.

- (a) Ridge 선형 프로브, (b) 방향/크기 분리, (c) 단조성은 주로 H1~H3 근방을 검증한다.
- (d) 사상 모델(residual MLP)은 H1~H5 전부를 특수해로 포괄하는 구조라, 위계 전체를 한
  번에 검증한다 (아래 "(d) 사상 모델 학습" 참고).
- 모든 측정에서 통제 기준선을 이기지 못하면 H6(=사실상 H0)으로 판정한다.

**어느 칸에서 잡히든 유효한 결과다.** 결론을 특정 칸으로 유도하지 말 것.

---

## 환경 제약

- **MacBook, Apple M5 기본형** (10코어 CPU / 10코어 GPU, 통합메모리 16~32GB)
- GPU 클러스터 없음. **TokenSynth 재학습은 시도하지 않는다.** 추론과 분석만.
  (단, (d) 사상 모델은 CLAP 임베딩→임베딩만 다루는 작은 MLP이므로 이 제약과 무관하게
  M5 CPU로 수 분 내 학습 가능 — 아래 "(d) 사상 모델 학습" 참고)
- Python / PyTorch 설치되어 있음
- **기본 device는 `cpu`.** CLAP forward는 가볍고, MPS 백엔드는 빠진 연산이 있어 첫 시도에서 막히는 경우가 많다. `--device mps` 옵션은 두되 기본값으로 삼지 말 것. MPS 사용 시 `PYTORCH_ENABLE_MPS_FALLBACK=1` 안내를 README에 넣을 것.

---

## 입력

**CLAP 체크포인트** — TokenSynth가 쓴 것과 **반드시 동일**해야 한다.

```
music_audioset_epoch_15_esc_90.14.pt
https://huggingface.co/lukewys/laion_clap/resolve/main/music_audioset_epoch_15_esc_90.14.pt
```

로딩: `laion_clap.CLAP_Module(enable_fusion=False, amodel='HTSAT-base')` → `load_ckpt(...)`
(구버전은 `device` 인자를 안 받으므로 `TypeError` fallback 처리할 것)

**소스 오디오** — 임의의 wav 폴더를 받되, NSynth **test split**(약 4,096개)을 상정.
전체 train split(20GB+)은 이 실험에 불필요하다.
NSynth 파일명에서 악기명과 pitch를 파싱할 것: `bass_synthetic_033-045-100.wav` → 악기 `bass_synthetic_033`, pitch `45`

---

## 파이프라인

```
소스 오디오 N개 (기본 300)
     │
     ├─ dry (원본)
     ├─ Reverb      × 7 레벨
     ├─ Distortion  × 7 레벨
     └─ HighShelf   × 7 레벨
     │                          → 소스당 22개 조건
     ▼  CLAP (48kHz → HTSAT → 512차원)
     ▼
  임베딩 행렬 [N×22, 512]
     │
     ├─ (a) Ridge 선형 프로브 → R²
     ├─ (b) 차이 벡터 방향/크기 분리 → 방향 코사인 + 크기 Spearman ρ
     ├─ (c) 단조성 → Spearman ρ
     └─ (d) 사상 모델(residual MLP) 학습 → held-out cos(e', e_wet)
              └─ (e) 역방향 사상 [선택 과제, 이번 구현에는 미포함]
```

### 스크립트 3개로 분리

| 파일 | 역할 |
|---|---|
| `01_embed.py` | 이펙트 적용 + 임베딩 추출 → `out/embeddings.npz` |
| `02_analyze.py` | (a)(b)(c) 분석 + 그림 → `out/results.json`, `out/probe_r2.png`, `out/direction_cos.png`, `out/monotonicity.png` |
| `03_mapping.py` | (d) 사상 모델 학습·평가 → `out/results.json`에 위계 결과 추가, `out/mapping_cos.png`, `out/hierarchy.png` |

`03_mapping.py`는 `02_analyze.py`가 만든 `results.json`을 읽어 위계(H0~H6) 결과를 추가하는
방식으로 이어 붙인다 (덮어쓰지 않음).

**오디오는 디스크에 쓰지 말 것.** 메모리에서 처리하고 임베딩만 저장한다.
(300×22 임베딩 ≈ 13MB. 오디오로 저장하면 수 GB.)

---

## 이펙트 스펙 (pedalboard 사용)

| 이펙트 | 파라미터 | 범위 | 레벨 수 |
|---|---|---|---|
| `reverb` | `Reverb(room_size=v, wet_level=0.4, dry_level=0.6)` | 0.0 → 0.9 | 7 |
| `distortion` | `Distortion(drive_db=v)` | 0 → 30 | 7 |
| `highshelf` | `HighShelfFilter(cutoff_frequency_hz=4000, gain_db=v)` | −15 → +15 | 7 |

`np.linspace`로 균등 분할. 각 스윕의 첫 레벨이 "무효과"에 해당하도록 잡아 dry와 연속되게 할 것.

---

## 오디오 전처리 — 반드시 지킬 것

1. **48kHz 리샘플 필수.** LAION-CLAP은 48kHz를 기대한다. NSynth는 16kHz라 반드시 변환해야 한다.
2. **모노 변환**, 고정 길이 4초 (짧으면 zero-pad, 길면 자름)
3. **피크 정규화** (0.7 정도). 이걸 안 하면 이펙트 파라미터와 음량이 뒤엉켜 프로브가 음량을 학습해버린다.
4. **무음 파일 제외.** `abs(y).max() < 1e-4`면 스킵. 차이 벡터 계산이 불안정해진다.
5. **이펙트 적용 후 클리핑 방지.** 피크가 1.0을 넘으면 0.99로 재정규화.

---

## 분석 스펙

### (a) 선형 프로브

Ridge regression: 임베딩(512d) → 이펙트 파라미터 값. 이펙트별로 따로.

> **⚠️ 가장 중요한 함정 — train/test를 `src_id` 단위로 분리할 것.**
> `sklearn.model_selection.GroupShuffleSplit(groups=src_id)` 사용.
> 같은 소스의 dry/wet이 train과 test에 걸치면 모델이 이펙트가 아니라 **소스를 외운다.**
> 무작위 split을 쓰면 R²가 허위로 높게 나오고 실험 전체가 무의미해진다.

held-out R²를 보고할 것.

### (b) 차이 벡터 — 방향과 크기 분리 (개정)

기존에는 `v_i = e_wet_i − e_dry_i`의 쌍별 코사인 하나만 봤다. 이는 "방향이 모두
같은가"(H1)만 검증하는 셈이라, 크기는 소스마다 달라도 방향은 고정인 H2 같은 경우를
H0으로 잘못 떨어뜨린다. 다음처럼 방향과 크기를 나눠서 측정한다.

① **방향**: `v̂_i = v_i / ‖v_i‖`로 정규화한 뒤, 같은 이펙트·같은 레벨에서 서로 다른 소스
간 평균 쌍별 코사인 유사도를 측정한다.

② **크기**: `‖v_i‖`와 파라미터 값 `p`의 관계를 Spearman ρ로 측정한다 (세기가 커질수록
이동량도 커지는가).

> **⚠️ 정규화를 반드시 먼저 할 것.** 정규화 없이 코사인을 재면 소스마다 다른 벡터 크기가
> 방향 불일치로 오인되어 H2를 H0으로 잘못 판정하게 된다. **이 한 줄 차이로 결론이
> 뒤집힌다.**

### (c) 단조성

각 이펙트의 평균 방향 벡터에 임베딩을 투영하고, 파라미터 값과의 **Spearman ρ**를 계산한다.
세기 조절이 가능한지를 본다. (참고: `highshelf`처럼 파라미터 범위가 dry를 사이에 두고
대칭인 경우, signed ρ가 낮게 나올 수 있으니 `|파라미터|` 기준 ρ도 함께 보고할 것.)

### (d) 사상 모델 학습 — CLAP 임베딩 → CLAP 임베딩 (신규)

CLAP 임베딩에서 CLAP 임베딩으로의 매핑을 직접 학습해 위계(H0~H6) 전체를 한 번에
검증한다. TokenSynth를 통과시키지 않으므로 학습이 매우 싸다 (M5 CPU로 수 분). 이펙트를
우리가 직접 걸기 때문에 정답 레이블(`e_wet`)이 공짜이고 데이터를 얼마든지 생성할 수 있다.

- **입력**: `e_dry` (512) ⊕ 이펙트 원핫 (3) ⊕ 파라미터 값 (1) = 516차원
- **출력**: `Δ` (512차원)
- **최종 예측**: `e' = e_dry + Δ` ← **residual 파라미터화** (아래 이유 참고)
- **구조**: MLP 516 → 1024 → 1024 → 512, GELU + LayerNorm
- **손실**: MSE + 코사인 손실 병용
- **평가**: held-out에서 `cos(e', e_wet)`
- **train/test 분리**: 여기서도 **src_id 기준 `GroupShuffleSplit`**을 쓸 것 (완화 금지)

> **residual 파라미터화를 쓰는 이유**: H1~H4가 전부 이 모델의 특수해가 된다 — `Δ`가
> 소스·파라미터 무관 상수면 H1, `f(p)·v` 형태면 H2, 파라미터에만 의존하면 H3, `e_dry`에
> 선형으로 의존하면 H4에 대응한다. 그래서 사다리를 따로 올라갈 필요 없이 모델이 가능한
> 가장 단순한 구조를 스스로 찾는다. 학습 안정성도 residual 쪽이 더 좋다.

### (e) 역방향 사상 — 선택 과제 (신규, 이번 구현에는 미포함)

`e_wet → (이펙트 종류, 파라미터 값)`

소리를 듣고 무슨 이펙트가 얼마나 걸렸는지 역으로 추정하는 분류(이펙트 종류) + 회귀
(파라미터 값) 헤드. 정방향 사상 (d)이 잘 되면 자연스러운 확장이므로, 이번 단계에서는
**항목만 남겨두고 구현하지 않는다.**

### 통제 장치 — (a)(b)(c) 공통, 세 개 모두 반드시 포함

| 통제 | 방법 | 기대 |
|---|---|---|
| **레이블 셔플** | 파라미터 레이블을 무작위로 섞고 같은 프로브 | R² ≈ 0 |
| **무작위 벡터 쌍** | 512차원 무작위 벡터 쌍의 코사인 | ≈ 0 |
| **악기 분류 (상한)** | 임베딩 → 악기 라벨 분류 정확도 | 논문 기준 90.4% |

**악기 분류가 상한 기준선이다.** 이펙트 프로브가 여기 한참 못 미치면 "CLAP은 악기는 알지만 이펙트는 모른다"가 정량적으로 증명된다. 이 대조가 결과 해석의 핵심이므로 빠뜨리지 말 것.

### 사상 모델 (d)의 기준선 — 3종 (신규)

| 기준선 | 방법 | 의미 |
|---|---|---|
| **identity** | `e' = e_dry`를 그대로 출력 | 이걸 못 이기면 아무것도 학습하지 않은 것. `cos(e_dry, e_wet)`은 원래도 높게 나오므로, 이 기준선 없이는 착시가 생긴다 |
| **셔플** | 레이블을 섞고 **동일 용량의 모델**로 학습 | 절대값이 아니라 진짜 레이블과의 격차만 신뢰할 것. MLP는 정보가 없어도 외운다 — 용량을 맞추지 않으면 통제가 무력화된다 |
| **악기 분류 (상한)** | 임베딩 → 악기 라벨 분류 정확도 | 논문 기준 90.4% (위 공통 통제와 공유하는 상한 기준선) |

**"동일 용량" 조건을 반드시 지킬 것.** 프로브(사상 모델)가 강력해질수록 통제의 중요성도
커진다는 점을 README 해석 가이드에도 적을 것.

---

## 2단계 — 상한 확인 (이번 구현 범위 밖)

> **진짜 wet 오디오 → CLAP → 임베딩 → TokenSynth → 실제로 wet 소리가 나는가?**

이것이 사상 모델 성능의 상한이다. 진짜(실측) 임베딩으로도 TokenSynth가 wet을 재현하지
못한다면, 우리가 사상 모델로 만든 근사 임베딩으로는 당연히 안 된다.

- **낸다** → 임베딩에 정보가 있고 TokenSynth도 그 정보를 읽는다. `TokenSynth-Aug`의
  실패는 학습 문제였다는 뜻.
- **못 낸다** → 조건화(conditioning) 구조 자체를 바꿔야 한다는 뜻.

**이유**: 사상 모델이 실제 wet 임베딩과 아무리 가까운 벡터를 만들어도, 그 벡터가 실제
오디오에서 나올 수 있는 영역 밖(off-manifold)이면 TokenSynth가 못 알아듣는다. TokenSynth는
진짜 오디오에서 나온 임베딩만 보고 학습했기 때문이다.

**이번 구현에는 포함하지 말 것.** 1단계 결과를 본 뒤 진행할 후속 항목으로만 명시해 둔다.

---

## 출력

```
out/
├── embeddings.npz      임베딩 + 메타(src_id, effect, param, instrument, pitch)
├── embed_config.json   재현용 설정 기록
├── results.json        모든 수치 — H0~H6 위계 각 칸의 성적 포함
├── probe_r2.png         이펙트별 R² vs 셔플 기준선 vs 악기 상한
├── direction_cos.png    방향 코사인 vs 무작위 기준선, 크기-파라미터 Spearman ρ
├── monotonicity.png     파라미터 vs 투영값 산점도
├── mapping_cos.png      신규 — 사상 모델 성능 vs identity vs 셔플 기준선
└── hierarchy.png        신규 — H1~H5 각 칸의 성적 비교 (사다리 시각화)
```

`results.json`에는 판정에 필요한 수치를 전부 담을 것 — 이펙트별 R², 방향 코사인 평균/표준편차,
크기-파라미터 Spearman ρ, 단조성 Spearman ρ, 사상 모델 held-out 코사인, 세 가지 기준선
값(identity/셔플/악기분류), 그리고 **H0~H6 중 어느 칸에서 구조가 잡히는지**. `results.json`
파일 하나만 보고 이 판정을 내릴 수 있어야 한다.

---

## 코드 요구사항

- `argparse`: `--audio-dir`, `--n-sources`(기본 300), `--batch-size`(기본 16), `--device`(기본 cpu), `--seed`(기본 0), `--out`
- **시드 고정**으로 재현 가능하게
- `tqdm` 진행률 표시 (CPU에서 10~25분 걸리므로 필수)
- 배치 처리로 CLAP 호출 (한 개씩 하면 매우 느림)
- 체크포인트 없을 때 안내 메시지와 함께 종료
- 주석은 한국어

---

## 완료 기준

1. `01_embed.py`가 300 소스 기준 M5 CPU에서 30분 내 완주
2. `02_analyze.py`가 (a)(b)(c) 측정 (방향/크기 분리 포함) + 공통 통제 세 가지를 모두 산출
3. `03_mapping.py`가 사상 모델(d)을 src_id 기준으로 학습·평가하고, identity/셔플(동일 용량)/악기분류 세 기준선과 비교
4. `results.json`만 보고 H0~H6 중 **"어느 칸에서 구조가 잡히는가"**를 판정할 수 있을 것
5. README에 설치·체크포인트 다운로드·실행·위계 해석 가이드(표 포함) 포함
6. **결과 해석은 코드가 단정하지 말 것.** 수치를 내고, 해석 기준(위계표 포함)은 README에 표로 제시.
7. 2단계("상한 확인")는 이번 완료 기준에 포함되지 않는다 — 1단계 결과를 본 뒤의 후속 과제로만 남긴다.

---

## 하지 말 것

- **1단계에서는** TokenSynth 모델 자체를 건드리지 않는다 (2단계 항목 — 위 "2단계 — 상한 확인" 참고. 그 전까지는 이번 실험 범위 밖)
- 재학습, fine-tuning
- 오디오 파일을 디스크에 저장
- 무작위 train/test split (src_id 기준 `GroupShuffleSplit` 사용 — 위 함정 참조)
- 결과를 특정 방향(혹은 특정 위계 칸)으로 유도하는 코드나 주석
